<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/profileUpdate.css">
    <title>Document</title>
</head>
<body>
    <form action="/user/postProfile" method="post">
        <label for="name">name:</label>
        <input type="text" id="name" name="name" required value="<%= user.name%>">

        <label for="email">email:</label>
        <input type="text" id="email" name="email" required value="<%= user.email%>">
 

        <label for="DOB">Date of Birth:</label>
        <input type="date" id="DOB" name="DOB" required value="<%= Profile.length >0 && Profile[0].profileDetails.length >0 ? Profile[0].profileDetails[0].DOB :'' %>">

        <label for="age">Age:</label>
        <input type="number" id="age" name="age" required  value="<%= Profile.length >0 && Profile[0].profileDetails.length >0 ? Profile[0].profileDetails[0].age :'' %>">

        <label for="Gender">Gender:</label>
        <input type="text" id="Gender" name="Gender" required value="<%= Profile.length >0 && Profile[0].profileDetails.length >0 ? Profile[0].profileDetails[0].Gender :'' %>">

        <label for="PhoneNumber">Phone Number:</label>
        <input type="number" id="PhoneNumber" name="PhoneNumber" required value="<%= Profile.length >0 && Profile[0].profileDetails.length >0 ? Profile[0].profileDetails[0].PhoneNumber :'' %>">

        <button type="submit">Submit</button>
    </form>
</body>
</html>